int main(void)
{
	int i;
	while (i < 0)
	{
		return (val);
	}
	else if (toto
		&& tata || ( yoyo
			|| tutu)
		&& tata)
		if (che)
			return (void);
	else if (toto && tata || toutou
		|| toto)
	{
		a = a + (((a
						+ b))
				+ c);
		return ("toto");
	}
	else if (woa)
	{
		print ('wolabrute');
	}
}
